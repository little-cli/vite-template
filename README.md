#### 基本说明

```sh
  # vite vuex@next vue-router@4 vue3 eslint prettier 项目基础配置

  #husky lint-staged 代码提交规范

  #"lint-staged": {
  #  "*.{vue,js,ts}": "eslint --fix"
  # }, 只对暂存区的文件进行 eslint --fix

  # 全局安装 commitizen git cz才能生效
  # git cz 触发 commitizen 出发 git commit 校验规则
```